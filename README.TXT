With this program you can automatically clock out employees out time station by using Selenium.
The main app is Selenium_app.py. 
In Selenium_app you need to place the password and the User name.
Also you going to need to give a list of names. 
I use get_names.py to created a list of names and send it to Selenium_app.py best.